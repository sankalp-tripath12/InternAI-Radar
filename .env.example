# InternAI Radar uses per-service environment files:
#   backend/.env.example  -> copy to backend/.env
#   frontend/.env.example -> copy to frontend/.env.local
#
# Do not put real secrets in this root file.
