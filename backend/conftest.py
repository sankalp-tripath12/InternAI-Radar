# Having this file at the backend/ root ensures pytest treats this
# directory as the rootdir and adds it to sys.path, so test files
# can import from `app.*` the same way the running application does.
